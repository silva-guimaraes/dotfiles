---
- name: install
  become: true
  when: pacman_install
  community.general.pacman:
    state: present
    name:
      - zsh

- name: symlink config
  ansible.builtin.file:
    src: ~/dotfiles/roles/zsh/files/zshrc
    path: ~/.zshrc
    state: link
