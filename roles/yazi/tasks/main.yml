---
- name: install
  become: true
  when: "{{ pacman_install }}"
  community.general.pacman:
    state: present
    name:
      - yazi

- name: symlink config
  ansible.builtin.file:
    src: ~/dotfiles/roles/yazi/files
    path: ~/.config/yazi
    state: link
